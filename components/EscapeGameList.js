import React from 'react';
import { View, Button } from 'react-native';

const EscapeGameList = () => {
  // Escape Game List component content
  return (
    <View>
      <Button title="Go to Escape Game List" />
    </View>
  );
};

export default EscapeGameList;
